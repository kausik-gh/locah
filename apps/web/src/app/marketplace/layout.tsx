import type { ReactNode } from 'react'
import './marketplace.css'

/** Marketplace styles load here, on Marketplace routes only. */
export default function MarketplaceLayout({ children }: { children: ReactNode }) {
  return children
}
