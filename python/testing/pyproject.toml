[project]
name = "platform-testing"
version = "0.1.0"
description = "Platform Testing Shared Backend Library"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "platform-core",
    "sqlalchemy[asyncio]>=2.0.25",
    "asyncpg>=0.29.0",
]

[tool.uv.sources]
platform-core = { workspace = true }
